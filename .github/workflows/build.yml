---
name: Build (reusable)

on:
  workflow_call:
    inputs:
      distro:
        required: true
        type: string
      flavor:
        required: true
        type: string
      target:
        required: true
        type: string
      arch:
        required: true
        type: string
      runner:
        required: true
        type: string
      fast_build:
        required: false
        type: boolean
        default: false

jobs:
  build:
    runs-on: ${{ inputs.runner }}
    container:
      image: debian:trixie
    env:
      DEBIAN_FRONTEND: noninteractive
    steps:
      - name: Install prerequisites
        run: |
          apt-get update
          apt-get install -y --no-install-recommends \
            git \
            sudo \
            ca-certificates \
            adduser

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          path: derivative-maker
          submodules: recursive

      - name: Create build user
        run: |
          useradd -m user
          echo 'user ALL=(ALL) NOPASSWD: ALL' > /etc/sudoers.d/user
          chown -R user:user derivative-maker

      - name: Build gateway
        if: inputs.target != 'iso'
        run: |
          cd derivative-maker
          su user -c './derivative-maker \
            --flavor ${{ inputs.distro }}-gateway-${{ inputs.flavor }} \
            --target ${{ inputs.target }} \
            --type vm \
            --arch ${{ inputs.arch }} \
            ${{ inputs.fast_build && '--repo true --remote-derivative-packages true' || '' }}'

      - name: Build workstation
        if: inputs.target != 'iso'
        run: |
          cd derivative-maker
          su user -c './derivative-maker \
            --flavor ${{ inputs.distro }}-workstation-${{ inputs.flavor }} \
            --target ${{ inputs.target }} \
            --type vm \
            --arch ${{ inputs.arch }} \
            ${{ inputs.fast_build && '--repo true --remote-derivative-packages true' || '' }}'

      - name: Build ISO
        if: inputs.target == 'iso'
        run: |
          cd derivative-maker
          su user -c './derivative-maker \
            --flavor ${{ inputs.distro }}-${{ inputs.flavor }} \
            --target iso \
            --type host \
            --arch ${{ inputs.arch }} \
            ${{ inputs.fast_build && '--repo true --remote-derivative-packages true' || '' }}'

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.distro }}-${{ inputs.flavor }}-${{ inputs.target }}-${{ inputs.arch }}
          path: ~/derivative-binary/
          retention-days: 30
