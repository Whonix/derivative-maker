---
name: Build (reusable)

on:
  workflow_call:
    inputs:
      distro:
        required: true
        type: string
      flavor:
        required: true
        type: string
      target:
        required: true
        type: string
      arch:
        required: true
        type: string
      runner:
        required: true
        type: string
      fast_build:
        required: false
        type: boolean
        default: false

jobs:
  build:
    runs-on: ${{ inputs.runner }}
    env:
      DEBIAN_FRONTEND: noninteractive
      CI: true
    steps:

      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          path: derivative-maker
          submodules: recursive

      - name: Install build prerequisites
        run: |
          sudo add-apt-repository -y universe
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            sq \
            safe-rm \
            approx \
            debian-archive-keyring \
            fasttrack-archive-keyring \
            equivs

          ## gpg-sq is not in Noble repos. Create a dummy package so
          ## derivative-maker's apt check thinks it's installed, then
          ## symlink gpg as gpg-sq for the actual binary.
          echo "Section: misc
          Priority: optional
          Standards-Version: 4.0.0
          Package: gpg-sq
          Version: 99.0
          Description: dummy gpg-sq package (gpg symlink)" > /tmp/gpg-sq.equivs
          cd /tmp && equivs-build gpg-sq.equivs
          sudo dpkg -i /tmp/gpg-sq_99.0_all.deb
          sudo ln -sf /usr/bin/gpg /usr/local/bin/gpg-sq

      - name: Build Whonix gateway
        if: inputs.distro == 'whonix' && inputs.target != 'iso'
        run: |
          cd derivative-maker
          ./derivative-maker \
            --flavor whonix-gateway-${{ inputs.flavor }} \
            --target ${{ inputs.target }} \
            --type vm \
            --arch ${{ inputs.arch }} \
            --allow-untagged true \
            --allow-uncommitted true \
            ${{ inputs.fast_build && '--repo true --remote-derivative-packages true' || '' }}

      - name: Build Whonix workstation
        if: inputs.distro == 'whonix' && inputs.target != 'iso'
        run: |
          cd derivative-maker
          ./derivative-maker \
            --flavor whonix-workstation-${{ inputs.flavor }} \
            --target ${{ inputs.target }} \
            --type vm \
            --arch ${{ inputs.arch }} \
            --allow-untagged true \
            --allow-uncommitted true \
            ${{ inputs.fast_build && '--repo true --remote-derivative-packages true' || '' }}

      - name: Build Kicksecure
        if: inputs.distro == 'kicksecure'
        run: |
          cd derivative-maker
          ./derivative-maker \
            --flavor kicksecure-${{ inputs.flavor }} \
            --target ${{ inputs.target }} \
            --type ${{ inputs.target == 'iso' && 'host' || 'vm' }} \
            --arch ${{ inputs.arch }} \
            --allow-untagged true \
            --allow-uncommitted true \
            ${{ inputs.fast_build && '--repo true --remote-derivative-packages true' || '' }}

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: ${{ inputs.distro }}-${{ inputs.flavor }}-${{ inputs.target }}-${{ inputs.arch }}
          path: ~/derivative-binary/
          retention-days: 30
